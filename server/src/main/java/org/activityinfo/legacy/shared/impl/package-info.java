/**
 * Common (server/offline) implementation of the part of the API that can run on
 * either the server or the browser using an asynchronous sql legacy based
 * on WebSQL.
 */
package org.activityinfo.legacy.shared.impl;